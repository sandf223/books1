import React from "react";
import './SearchPanel.css';

const SearchPanel = () => {
    return (
        <input className='input-search' placeholder="Search" id="search"/>
    );
};

export default SearchPanel;
